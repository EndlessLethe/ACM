#include <stdio.h>


void dfs(int x, int y);
int res[1000] = {0};
int n, m, d;


int main() 
{
	scanf("%d %d %d", n, m, d);
	while (n != 0 && m != 0 && d != 0) {
		
	}
}


void dfs(int x, int y)
{
	
}
